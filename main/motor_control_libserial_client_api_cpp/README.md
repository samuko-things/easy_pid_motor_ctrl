## Install LibSerial Library For Serial Communication On Linux

````
sudo apt-get update
````

````
sudo apt install libserial-dev
````

</br>

## Stand alone make, build and run

````
cmake -B ./build/
````
````
cmake --build ./build/
````
````
./build/libserial_client_api 
````